module model.packets.packet_key;

immutable int USER_CONNECT_PACKET = 0;
immutable int DRAW_COMMAND_PACKET = 1;
immutable int UNDO_COMMAND_PACKET = 2;
immutable int CHAT_MESSAGE_PACKET = 3;
immutable int CANVAS_SYNCH_PACKET = 4;